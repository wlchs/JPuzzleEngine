package com.laszloborbely.jpuzzle.core.strategy;

/**
 * Interface of puzzle solving strategies
 */
public interface IStrategy {
    /**
     * Executes selected solving strategy and writes the results to the output handler
     */
    void execute();
}
